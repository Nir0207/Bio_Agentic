services:
  gds:
    build:
      context: ..
      dockerfile: gdsGraph/Dockerfile
    working_dir: /app/gdsGraph
    env_file:
      - ../.env
    volumes:
      - ../:/app
    extra_hosts:
      - "host.docker.internal:host-gateway"
    command: ["python", "-m", "gds.app.cli", "run", "all"]
